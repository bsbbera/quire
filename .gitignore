# Heavy working data, not source. Backed up separately, never versioned here.
Books/
Magazine/
design/
site/
radar/
worlds/
desktop/node_modules/
desktop/src-tauri/target/
desktop/src-tauri/gen/
desktop/build.log
_backups/
node_modules/
*.afpub
*.afpub~lock~
*.png
*.jpg
.inkos/

# Secrets and browser/agent profile state — never versioned.
.env
.env.*
*.key
.window-profile/
.agents/
.impeccable/
.claude/settings.local.json

# Its own git repo with a GitHub remote — versioned there, not here.
ideaverse-skills/

# Signing keys live in ~/.quire, never in the repo.
*.key
*.key.pub

# Machine-specific dev preview config.
.claude/launch.json

# The bundler needs these; the blanket *.png above is for artwork, not icons.
!desktop/src-tauri/icons/**/*.png

# Build output: the InkOS fork staged for the installer by
# desktop/vendor-studio.mjs. ~100 MB, regenerated from vendor/studio.
cli-shim/engine/

# The dev install lives in the checkout: build-dev.mjs drops the binary here.
/quire.exe
/.env
